#include<iostream>
 
using namespace std;
 

int main()
{

    int n;
    cin >> n;
    
    int first_digit;
    cin >> first_digit;
    int second_digit;
    cin >> second_digit;

    int numbs[n];

    for (size_t i = 0; i < n; i++)
    {
        cin >> numbs[i];
    }

    int difference = counter(first_digit, 0, numbs);

    cout<<"Hello World";
     
    return 0;
}


int counter(int first_digit, int difference, int numbs[]){

    int n = 

    switch (n)
    {
    case /* constant-expression */:
        /* code */
        break;
    
    default:
        break;
    }
}